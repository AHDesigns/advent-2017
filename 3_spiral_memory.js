function generateSpiral(){
        const spiral = [];
        const origin = 1;
        for (var x = 0; x < 6; x++) {
               spiral[x] + 
        }
}

function generateArray(x) {
        var i = 1;
        return 1;
}


function check(input, expected) {
        if(generateArray(input) === expected) {
                console.log('true for: ', input, ' = ', expected);
        } else {
                console.log('failed for: ', input, ' result was: ', generateArray(input), ' expected: ', expected);
        }
}

check(1,1);
check(8,2);
check(22,3);

